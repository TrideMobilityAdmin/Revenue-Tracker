# Base URL of the backend API. Use the local backend during development,
# or the deployed API URL in production.
# VITE_API_URL=http://localhost:5000/api
VITE_API_URL=https://revenue-tracker-api.tride.live/api
